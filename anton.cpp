#include <bits/stdc++.h>
using namespace std;

int main() 
{
    char str[20];
    for(int i=1; str[i]!='\0'; i++)
    {
        cin>>str[i];
    }
    for(int i=1; str[i]!='\0'; i++)
    {
        cout<<str[i];
    }

    return 0;
}